<@pp.dropOutputFile />
<@pp.changeOutputFile name="output__"+tenant.code+"__"+source.code_+"_"+stream.code+".xml" />
{
  "name": "output__${tenant.code}__${source.code_}__${stream.code}",
  "version": "1.0.0",
  "nickName": "output__${tenant.code}__${source.code_}__${stream.code}",
  "description": "Stream output__${tenant.code}__${source.code_}__${stream.code}",
  "metaData": [
    {
      "name": "source",
      "type": "STRING"
    }
  ],
  "payloadData": [
    {
      "name": "time",
      "type": "STRING"
    },
    <#list components as component>
     { 
      "name": "${component.code}",
      "type": "${component.type}"
     }<#if component_has_next>,</#if>
     </#list>
  ]
}